version: '3.8'

services:
  app:
    image: 360smartconnect/gs1_decoder_api:lastest
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 8000
    labels:
      - traefik.enable=true
      - "traefik.http.routers.http-0-tos8ck0k4kw44o80sg8s0g84-app.rule=Host(`gs1-decoder-api.rorworld.eu`) && PathPrefix(`/`)"
      - traefik.http.routers.http-0-tos8ck0k4kw44o80sg8s0g84-app.entryPoints=http